// Definition of the pokemon object
var pokemon = {
  name,
  heigth,
  types: ["Electric", "Fire", "Water"]
}
// Initialized 3 objects
pokemon = {"pikachu", 0.4, types[1]}
pokemon = {"charmander", 0.6, types[2]}
pokemon = {"squirtle", 0.5, types[3]}
// Declared variables for the loop
var i;
var repository[i] = [pokemon];
